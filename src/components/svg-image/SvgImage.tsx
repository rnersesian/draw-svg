import React, {PureComponent, HTMLFactory} from "react"


interface Props {
    image?: HTMLFactory<SVGElement>;
    style?: React.CSSProperties;
    viewBox?: String;
}

export class svgImage extends React.Component<Props> {

    render = () => {
        
        return (
            
        )
    }
}